apiVersion: v1
kind: Service
metadata:
  name: notes-vault-mcp
  labels:
    app: notes-vault-mcp
spec:
  selector:
    app: notes-vault-mcp
  ports:
    - name: http
      port: 8765
      targetPort: http
